# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code remember plugin session memory (local only)
.remember/

# Verification review output
review/

# Puppeteer verify artifacts (regenerated by `npm run verify`)
scripts/screenshots/
scripts/verify-report.json

# Brand capture artifacts (regenerated by `npm run capture:brand`)
scripts/brand-capture/

# Git worktrees (isolated workspaces)
.worktrees/

# WP plugin build output (regenerated by `npm run build:plugin`)
wp-plugin/bracket-generator/dist/

# Plugin distribution zip (regenerated by build pipeline)
*.zip
